<h1>Insparational Movie Review App</h1>
<p>The list of all movies (<%= @movies.size%> movies):</p>

<ul>
    <% @movies.each do |movie| %>
        <li><strong><%= movie.name %></strong></li>
        in <%= movie.location %>:
        <%= number_to_currency(movie.price) %><br><br>
    <% end %>
</ul>
